# 项目代码规范

> **文档编号**: 04-项目代码规范  
> **版本**: V1.0  
> **更新日期**: 2026-08-18

---

## 一、代码风格规范

### 1.1 Python代码风格

遵循 [PEP 8](https://peps.python.org/pep-0008/) 规范：

```python
# ✅ 正确示例
class CrackDetector:
    """裂缝检测器类"""
    
    def __init__(self, model_path: str, confidence_threshold: float = 0.5):
        """
        初始化检测器
        
        Args:
            model_path: 模型文件路径
            confidence_threshold: 置信度阈值
        """
        self.model_path = model_path
        self.confidence_threshold = confidence_threshold
    
    def detect(self, image: np.ndarray) -> List[Dict]:
        """
        检测图像中的裂缝
        
        Args:
            image: BGR格式图像
        Returns:
            检测结果列表
        """
        pass
```

```python
# ❌ 避免的写法
class crackDetector:  # 类名应使用CamelCase
    def detect(self,img):  # 参数间应有空格
        pass  # 避免空行过少
```

### 1.2 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 模块名 | 小写+下划线 | `crack_detector.py` |
| 类名 | CamelCase | `CrackDetector` |
| 函数名 | 小写+下划线 | `detect_crack()` |
| 常量 | 大写+下划线 | `MAX_CONFIDENCE = 0.9` |
| 私有属性 | 单下划线前缀 | `_model_path` |

### 1.3 注释规范

```python
# 行内注释
x = x + 1  # 增加计数器 (不是必须)

# 块注释
# 计算裂缝宽度
# 1. 获取骨架点
# 2. 计算法线方向
# 3. 测量像素距离
width = calculate_width(skeleton_points)

# 文档字符串
def process_image(image: np.ndarray) -> Dict:
    """
    处理图像并返回检测结果
    
    Args:
        image: 输入图像 (BGR格式)
        
    Returns:
        检测结果字典，包含:
        - cracks: 裂缝列表
        - stats: 统计信息
        
    Raises:
        ValueError: 当图像为空时
    """
    pass
```

---

## 二、Git提交规范

### 2.1 提交消息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 2.2 Type类型

| Type | 说明 | 示例 |
|------|------|------|
| feat | 新功能 | `feat(perception): 添加U-Net分割算法` |
| fix | 修复bug | `fix(gateway): 修复WebSocket断连问题` |
| docs | 文档变更 | `docs: 更新技术实施方案V3.1` |
| style | 代码格式 | `style: 格式化Python代码` |
| refactor | 重构 | `refactor(detection): 重构裂缝检测模块` |
| test | 测试相关 | `test: 添加单元测试` |
| chore | 构建/工具 | `chore: 更新依赖版本` |

### 2.3 示例

```bash
# ✅ 正确示例
git commit -m "feat(perception): 添加YOLOv8裂缝检测算法

- 实现TensorRT模型加载
- 添加预处理和后处理逻辑
- 置信度阈值可配置

Closes #12"

# ❌ 避免的写法
git commit -m "fix bug"
git commit -m "updated"
```

---

## 三、目录结构规范

```
lite3-power-inspection/
├── README.md                    # 项目说明
├── docs/                        # 文档目录
│   ├── 00-参考资料/            # 官方文档
│   ├── 01-技术方案/            # 技术方案
│   ├── 02-竞赛文档/            # 竞赛相关
│   └── 03-项目管理/            # 项目管理
├── src/                         # 源代码
│   ├── __init__.py
│   ├── main.py                 # 主入口
│   ├── perception/             # 感知模块
│   │   ├── __init__.py
│   │   ├── yolo_detector.py
│   │   ├── unet_segmentor.py
│   │   └── temperature_monitor.py
│   ├── gateway/                # 通信网关
│   │   ├── __init__.py
│   │   ├── websocket_server.py
│   │   ├── udp_controller.py
│   │   └── ptz_controller.py
│   ├── app/                    # 应用服务
│   │   ├── __init__.py
│   │   ├── inspector.py
│   │   └── demo_orchestrator.py
│   └── config/                 # 配置
│       ├── __init__.py
│       ├── robot_config.yaml
│       └── inspection_config.yaml
├── models/                      # 模型文件
│   ├── yolov8s.trt
│   └── unet-crack.onnx
├── tests/                       # 测试代码
│   ├── __init__.py
│   ├── test_perception.py
│   └── test_gateway.py
├── scripts/                     # 脚本工具
│   ├── train.py
│   └── export_model.py
├── requirements/                # 依赖文件
│   ├── core.txt
│   ├── perception.txt
│   └── gateway.txt
└── data/                        # 数据目录
    ├── checkpoints/            # 模型检查点
    └── logs/                   # 运行日志
```

---

## 四、配置管理

### 4.1 YAML配置规范

```yaml
# 使用驼峰或下划线保持一致
detection:
  crack:
    yolo_model: "models/yolov8s.trt"
    confidence_threshold: 0.5
    min_width_mm: 0.1
  temperature:
    warn_threshold: 45.0
    critical_threshold: 50.0
```

### 4.2 环境变量

```bash
# 开发环境
export ROBOT_IP=192.168.1.103
export PTZ_IP=192.168.1.108
export SERVER_URL=ws://192.168.1.200:8765/ws

# 生产环境
export ROBOT_IP=${ROBOT_IP:-192.168.1.103}
```

---

## 五、错误处理规范

### 5.1 异常类定义

```python
class InspectionError(Exception):
    """巡检基础异常"""
    pass

class DetectionError(InspectionError):
    """检测异常"""
    pass

class CommunicationError(InspectionError):
    """通信异常"""
    pass

class HardwareError(InspectionError):
    """硬件异常"""
    pass
```

### 5.2 异常处理模式

```python
import logging

logger = logging.getLogger(__name__)

async def detect_crack(image: np.ndarray) -> List[Dict]:
    try:
        # 检测逻辑
        results = await self._run_inference(image)
        return results
    except ModelLoadError as e:
        logger.error(f"模型加载失败: {e}")
        raise DetectionError("无法执行检测") from e
    except Exception as e:
        logger.exception(f"未知错误: {e}")
        raise DetectionError(f"检测失败: {e}") from e
```

---

## 六、测试规范

### 6.1 测试文件命名

```
tests/
├── test_crack_detector.py      # 对应 src/perception/crack_detector.py
├── test_temperature_monitor.py
├── test_websocket_gateway.py
└── conftest.py                 # pytest配置
```

### 6.2 测试代码示例

```python
import pytest
import numpy as np

class TestCrackDetector:
    
    @pytest.fixture
    def detector(self):
        return CrackDetector(model_path="models/yolov8s.trt")
    
    def test_detect_empty_image(self, detector):
        """测试空图像处理"""
        image = np.zeros((100, 100, 3), dtype=np.uint8)
        results = detector.detect(image)
        assert results == []
    
    def test_detect_with_crack(self, detector, sample_image):
        """测试裂缝检测"""
        results = detector.detect(sample_image)
        assert len(results) > 0
        assert all(r["confidence"] >= 0.5 for r in results)
```

---

## 七、代码审查清单

- [ ] 代码遵循PEP 8规范
- [ ] 函数有文档字符串
- [ ] 异常处理合理
- [ ] 日志记录适当
- [ ] 测试覆盖率>80%
- [ ] 配置文件无硬编码
- [ ] Git提交信息规范

---

*文档版本: V1.0 | 更新日期: 2026-08-18*
