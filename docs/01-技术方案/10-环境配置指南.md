# 绝影Lite3 环境配置指南

> **文档编号**: DOC-CONFIG-001
> **版本**: V1.7
> **编制日期**: 2025-09-16
> **编制人**: 陈伟
> **适用范围**: 绝影Lite3电力巡检系统环境配置与硬件评估

---

## 一、感知主机环境配置

> 快速参考见 [03-环境配置说明](03-环境配置说明.md)，部署流程见 [06-部署指南](06-部署指南.md)

### 1.1 实际主机配置

| 项目 | 实际值 | 状态 |
|------|--------|------|
| 主机型号 | NVIDIA Jetson Xavier NX | ✓ 已具备 |
| 主机名 | `lite` | ✓ |
| CPU | 6核 ARMv8 | ✓ |
| 内存 | 6.7Gi（可用约5.4Gi） | ⚠️ 略低于推荐值 |
| GPU | NVIDIA Jetson Xavier NX（21 TOPS） | ✓ 已具备 |
| 操作系统 | Ubuntu 20.04.6 LTS | ✓ |
| Python版本 | 3.8.10 | ✓ |
| IP地址 | 192.168.1.103（静态） | ✓ |

### 1.2 软件环境检查

#### 已预装软件

| 软件 | 版本 | 用途 | 状态 |
|------|------|------|------|
| Python | 3.8.10 | 编程语言 | ✓ 已安装 |
| CUDA | 已安装 | GPU计算加速 | ✓ 已安装 |
| PyTorch | 已安装 | 深度学习框架 | ✓ 已安装 |
| numpy | 1.23.1 | 数值计算 | ✓ 已安装 |
| opencv-python | 4.9.0 | 图像处理 | ✓ 已安装 |
| requests | 2.31.0 | HTTP请求 | ✓ 已安装 |
| pyyaml | 5.3.1 | 配置解析 | ✓ 已安装 |

#### 需安装软件（Python依赖）

| 软件 | 版本要求 | 用途 | 大小预估 |
|------|----------|------|----------|
| loguru | >=0.6.0 | 日志管理 | ~200KB |
| websockets | >=12.0 | WebSocket通信 | ~150KB |
| fastapi | >=0.104.0 | Web框架 | ~600KB |
| uvicorn | >=0.24.0 | ASGI服务器 | ~300KB |
| pydantic | >=2.0.0 | 数据验证 | ~1.5MB |

**合计**: 约 **2.75 MB**

### 1.3 环境变量配置

创建 `~/.inspection_env` 文件：

```bash
# 项目路径
export PROJECT_HOME=~/lite3-power-inspection

# Python路径
export PYTHONPATH=${PROJECT_HOME}/src:${PYTHONPATH}

# 模型路径
export MODEL_PATH=${PROJECT_HOME}/models

# 日志路径
export LOG_PATH=${PROJECT_HOME}/data/logs
```

在 `~/.bashrc` 中添加：
```bash
source ~/.inspection_env
```

---

## 二、运动主机说明

| 项目 | 说明 |
|------|------|
| 主机型号 | RK3588 ARM处理器 |
| IP地址 | 192.168.1.103（与感知主机共享） |
| 部署需求 | **无需单独部署**，通过UDP协议(端口43893)与感知主机通信 |
| 主要职责 | 运动控制、步态规划、实时通信 |

> **重要说明**：绝影Lite3为单机双主机架构，感知主机与运动主机共享同一IP地址 `192.168.1.103`，通过不同端口区分服务（UDP 43893 / WebSocket 8765 / RTSP 554）。运动控制功能由绝影官方SDK提供，本项目仅需通过UDP协议与之通信。

---

## 三、硬件规格对比

### 3.1 感知主机（Jetson Xavier NX）

| 参数 | 规格值 | 单位 |
|------|--------|------|
| GPU架构 | Volta | - |
| CUDA Core数量 | 512 | 个 |
| Tensor Core数量 | 64 | 个 |
| GPU算力（FP16） | 21 | TOPS |
| GPU算力（INT8） | 42 | TOPS |
| 显存 | 8 | GB（LPDDR4） |
| 显存带宽 | 84.6 | GB/s |
| CPU | 6核 ARM Cortex-A78AE | - |
| CPU频率 | 最高 1.9 | GHz |
| 总内存 | 8 | GB（与GPU共享） |
| 存储接口 | M.2 Key M | - |
| 电源 | 15W（典型） | W |

### 3.2 运动主机（RK3588）

| 参数 | 规格值 | 单位 |
|------|--------|------|
| CPU架构 | ARM Cortex-A76/A55 | - |
| CPU核心数 | 8核（4×A76 + 4×A55） | 个 |
| CPU最高频率 | 2.4 | GHz |
| GPU | ARM Mali-G610 MP4 | - |
| GPU算力 | ~0.5 | TFLOPS |
| NPU算力 | 6 | TOPS |
| 总内存 | 8 | GB（LPDDR4/LPDDR5） |
| 存储接口 | eMMC / NVMe SSD | - |

> **注意**：RK3588的GPU性能有限，不适合运行深度学习推理。NPU（6 TOPS）可用于轻量级AI推理，但本项目主要在Jetson NX上运行。

---

## 四、外部设备配置

### 4.1 云台相机

| 配置项 | 值 |
|--------|-----|
| 设备型号 | 数尔安防SR-UPA810T609 |
| IP地址 | 192.168.1.108 |
| 账户 | admin / 123456 |
| RTSP端口 | 554 |
| HTTP端口 | 80 |
| 视频流 | `rtsp://admin:123456@192.168.1.108:554/id=1&type=0` |

### 4.2 网络拓扑

| 设备 | IP地址 | 端口 | 协议 | 用途 |
|------|--------|------|------|------|
| 感知主机 | 192.168.1.103 | 8765 | WebSocket | 数据上报 |
| 感知主机 | 192.168.1.103 | 8000 | HTTP | 监测平台 |
| 感知主机 | 192.168.1.103 | 8080 | HTTP | 图片服务 |
| 运动主机 | 192.168.1.103 | 43893 | UDP | 运动控制 |
| 云台相机 | 192.168.1.108 | 554 | RTSP | 视频流 |
| 监测平台 | 192.168.1.200 | 8765 | WebSocket | 数据接收 |

---

## 五、环境诊断工具

### 5.1 采集环境信息

```bash
cd ~/lite3-power-inspection

# 运行采集脚本（推荐）
./scripts/gather_info.sh

# 或运行Python脚本（详细输出）
source venv/bin/activate
python3 scripts/gather_system_info.py --output data/system_info_report.md
```

### 5.2 查看诊断报告

```bash
cat data/system_info_report.md
```

### 5.3 快速验证

```bash
# 验证核心依赖
python3 -c "import loguru, websockets, fastapi, uvicorn, pydantic; print('✅ 核心依赖已安装')"

# 验证GPU环境
python3 -c "import torch; print(f'PyTorch版本: {torch.__version__}'); print(f'CUDA可用: {torch.cuda.is_available()}')"

# 验证OpenCV
python3 -c "import cv2; print(f'OpenCV版本: {cv2.__version__}')"
```

---

## 六、常见问题排查

### 6.1 主机环境检查清单

```bash
# 1. 确认GPU环境
nvidia-smi
nvcc --version

# 2. 检查Python版本
python3 --version

# 3. 检查磁盘空间
df -h

# 4. 检查内存
free -h

# 5. 检查网络连接
ping -c 2 192.168.1.108  # 云台相机
ping -c 2 192.168.1.200  # 监测平台
```

### 6.2 依赖安装问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 依赖安装失败 | 无网络 | 使用离线包 |
| pip版本过低 | pip < 20.0 | `pip install --upgrade pip` |
| 镜像源问题 | 网络限制 | 使用国内镜像：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple` |

### 6.3 SSH登录问题

```bash
# 正确密码（英文单引号）
ssh ysc@192.168.1.103
# 密码: '

# 检查SSH服务
sudo systemctl status ssh
```

---

## 七、模式选择指南

### 7.1 模拟模式（推荐）

**适用场景**：
- 开发测试
- 赛前准备
- 算法验证

**运行要求**：
- Python 3.8+
- 9个核心依赖包
- 无需GPU

**运行命令**：
```bash
python3 scripts/demo_12min.py --mode simulation
```

### 7.2 真实模式

**适用场景**：
- 竞赛演示
- 生产环境

**运行要求**：
- GPU环境（Jetson Xavier NX）
- CUDA 11.4+
- PyTorch + CUDA支持
- TensorRT 8.0+
- 训练好的模型文件

**运行命令**：
```bash
python3 scripts/demo_12min.py --mode real
```

---

## 八、版本兼容性说明

| 项目 | 推荐版本 | 实际版本 | 兼容性 |
|------|----------|----------|--------|
| Python | 3.10+ | 3.8.10 | ✅ 兼容，可正常运行 |
| 内存 | 8GB+ | 6.7Gi | ⚠️ 可用约5.4Gi，建议关闭其他应用 |
| 操作系统 | 22.04 LTS | 20.04.6 LTS | ✅ 兼容 |
| CUDA | 12.x | 已安装 | ✅ 兼容 |

---

*文档版本: V1.7 | 最后更新: 2025-09-16 | 编制人: 陈伟*
