# 感知开发指南

> **版本号**: V2.2.3  
> **来源**: [绝影Lite3开发指南](https://alidocs.dingtalk.com/i/p/OlnXRxOLAljEbGLp/docs/93NwLYZXWyg6Rxw6tO6aOoppJkyEqBQm)

---

## 目录

1. [感知系统](#1-感知系统)
2. [准备工作](#2-准备工作)
   - 2.1 远程登录
     - 2.1.1 连接
     - 2.1.2 故障排查
   - 2.2 通过 HDMI 连接到感知主机
     - 2.2.1 设置图形化界面自启动
     - 2.2.2 恢复tty3自启动
3. [ROS版本查看与切换](#3-ros版本查看与切换)
4. [深度相机](#4-深度相机)
   - 4.1 相机驱动
   - 4.2 相机测试
   - 4.3 librealsense二次开发
   - 4.4 Realsense-ROS1二次开发
   - 4.5 Realsense-ROS2二次开发
5. [运动通信功能包](#5-运动通信功能包)
   - 5.1 功能介绍
   - 5.2 使用方法
     - 5.2.1 ROS1
     - 5.2.2 ROS2
   - 5.3 程序结构说明
     - 5.3.1 ROS1
     - 5.3.2 ROS2
6. [识别跟随功能包](#6-识别跟随功能包)
   - 6.1 功能介绍
   - 6.2 使用方法
   - 6.3 二次开发说明
7. [基于激光雷达的建图定位导航避障（ROS1）](#7-基于激光雷达的建图定位导航避障ros1)
   - 7.1 建图（适用于 v3.1.04 及以后）
     - 7.1.1 功能介绍
     - 7.1.2 程序结构
     - 7.1.3 使用方法
   - 7.2 建图（适用于 v3.1.04 以前）
     - 7.2.1 功能介绍
     - 7.2.2 程序结构
     - 7.2.3 使用方法
   - 7.3 定位导航
     - 7.3.1 单目标点定位导航使用方法
     - 7.3.2 多目标点定位导航使用方法
   - 7.4 二次开发说明
     - 7.4.1 雷达驱动二次开发
     - 7.4.2 SLAM建图二次开发
     - 7.4.3 定位导航二次开发
8. [基于激光雷达的建图定位导航避障（ROS2）](#8-基于激光雷达的建图定位导航避障ros2)
   - 8.1 建图
     - 8.1.1 功能介绍
     - 8.1.2 程序结构
     - 8.1.3 使用方法
   - 8.2 定位导航
     - 8.2.1 单目标点定位导航使用方法
     - 8.2.2 多目标点定位导航使用方法
   - 8.3 二次开发说明
     - 8.3.1 雷达驱动二次开发
     - 8.3.2 SLAM建图二次开发
     - 8.3.3 定位导航二次开发

---

## 1 感知系统

绝影Lite3专业版和激光版配备了感知主机NVIDIA Jetson Xavier NX，并配有感知算法案例，以便于用户进行二次开发。各传感器之间的连接情况如图所示，运动主机和感知主机通过UDP进行数据传输。

---

## 2 准备工作

### 2.1 远程登录

#### 2.1.1 连接

用户可通过NoMachine软件远程登录绝影Lite3感知主机桌面。方法如下：

1. 首先把开发主机连接到机器狗WiFi；
2. 打开NoMachine软件，输入感知主机IP地址 `192.168.1.103`；
3. 完成后页面将出现一个新的远程图标；
4. 点击连接，输入用户名 `ysc` 和密码 `'`（英文单引号）。

#### 2.1.2 故障排查

> **【注意】** 请在空旷处调试以防对人或物品造成损伤，遇到紧急情况，及时按下软急停，或关闭自主模式。

1. 若连接后远程桌面白屏，请点击NoMachine右上角的"Settings"，弹出设置页面后，选择左下角"Server Preferences"，再选择右上方"Updates"，点击"Check now"进行更新。更新完成后再次尝试远程连接。
2. 若输入密码后显示"session negotiation failed"，则需要在本地电脑SSH登录进行修复：
   ```bash
   ssh ysc@192.168.1.103     #密码是单引号
   rm xxxxx xxxxxx xxxxxx    #rm为删除指令，xxxxxx为目录下的各个文件名
   ```

### 2.2 通过 HDMI 连接到感知主机

绝影Lite3专业版/激光版支持通过背部HDMI口连接到感知主机桌面。感知主机开机时默认进入tty3终端，如需开机后自动进入图形化界面，需要进行相关设置。

#### 2.2.1 设置图形化界面自启动

1. 开机后按 `Alt+Ctrl+T` 打开终端；
2. 输入以下命令：
   ```bash
   cd /usr/share/X11/xorg.conf.d/
   sudo mv xorg.conf ..         #mv为移动指令，将xorg.conf文件移至上一目录
   ```
3. 重启机器狗；
4. 重启机器狗后，感知主机会自动进入图形化界面，如果未出现图形化界面，可能是因为系统未清除之前配置的缓存，请再次重启机器狗。

#### 2.2.2 恢复tty3自启动

将xorg.conf恢复至原路径并重启机器狗，即可恢复tty3自启动。方法如下：

1. 开机成功进入图形化界面后，按Alt+Ctrl+T按键打开终端窗口，并输入以下内容：
   ```bash
   cd /usr/share/X11/
   sudo mv xorg.conf xorg.conf.d/ #mv为移动指令，将xorg.conf文件移至xorg.conf.d/下
   ```
2. 重启机器狗；
3. 重启机器狗，即可恢复tty3自启动，开机后，感知主机将默认进入tty3终端。

---

## 3 ROS版本查看与切换

绝影Lite3从感知主机镜像V3.2.1版本开始同时支持ROS1和ROS2。

1. 查看当前ROS版本：
   ```bash
   cd /home/ysc/scripts
   sudo ./print_ros_version.sh
   ```
2. 切换到ROS1：
   ```bash
   cd /home/ysc/scripts
   sudo ./switch_ros_version.sh ros1
   ```
3. 切换到ROS2：
   ```bash
   cd /home/ysc/scripts
   sudo ./switch_ros_version.sh ros2
   ```
4. 切换成功后，请重启机器狗。

---

## 4 深度相机

绝影Lite3专业版和激光版配备了深度相机Intel RealSense D435i。

### 4.1 相机驱动

绝影Lite3的感知主机上已经安装了相应的深度相机驱动程序Intel RealSense SDK，用户可在终端输入以下命令行打开Intel官方提供的可视化程序：

```bash
realsense-viewer
```

点击Info按钮，可以看到更详细的参数信息，例如相机的固件版本、序列号等。展开Stereo Module或者RGB Camera，可以配置相机的分辨率、帧数等参数并进行实时效果查看。

### 4.2 相机测试

在使用相机驱动进行开发前，先检查深度相机是否正常连接：

1. 确认左侧栏目中已经显示Intel RealSense D435i；
2. 点击Stereo Module和RGB Camera的on/off开关，若成功打开深度图和彩色图，说明深度相机正常连接。

### 4.3 librealsense二次开发

librealsense相关库基于CUDA进行编译，已安装至`/usr/local/lib`和`/usr/local/include`下，若要进行相关二次开发可自行include对应头文件并链接对应库进行编译。

### 4.4 Realsense-ROS1二次开发

ROS1版本的realsense-ros包位于`/home/ysc/lite_cog/drivers/realsense_ws`目录下，相关功能可通过系统服务开启关闭和查看状态，对应命令行为：

```bash
sudo systemctl start realsense
sudo systemctl status realsense
```

命令将使用`~/lite_cog/drivers/realsense_ws/src/realsense2_camera/launch`文件夹下的`dr_camera.launch`文件进行realsense相机的启动，若需要修改相机的启动参数，可自行修改该launch文件。

### 4.5 Realsense-ROS2二次开发

ROS2版本的realsense-ros包位于`/home/ysc/lite_cog_ros2/driver/realsense_ws`目录下，相关功能可通过系统服务开启关闭和查看状态，对应命令行为：

```bash
sudo systemctl start realsense_ros2
sudo systemctl stop realsense_ros2
sudo systemctl status realsense_ros2
```

命令将使用`~/lite_cog_ros2/drivers/realsense_ws/src/realsense2_camera/launch`文件夹下的`dr_camera_launch.py`文件进行realsense相机的启动，若需要修改相机的启动参数，可自行修改该launch.py文件。

---

## 5 运动通信功能包

### 5.1 功能介绍

本案例实现了ROS与UDP消息的功能转换。

绝影Lite3的运动主机与感知主机之间、感知主机与手柄APP之间的数据传输均采用UDP协议，通过本案例提供的`message_transformer_cpp`软件包，可实现：

1. 将运动主机上报的UDP消息转换为ROS话题进行发布，将感知主机下发的运动控制指令使用UDP发送给运动主机；
2. 接收APP发送的控制指令，以开启和关闭感知主机上的AI功能。

**其提供的ROS通信接口：**

发布话题: 运动主机向感知主机传输数据

| 话题名 | 描述 | 消息类型 |
|--------|------|----------|
| `/leg_odom` | 腿部里程计 | `nav_msgs::Odometry` |
| `/imu/data` | IMU数据 | `sensor_msgs::Imu` |
| `/joint_states` | 关节数据 | `sensor_msgs::JointState` |

订阅话题: 感知主机向运动主机传输数据

| 话题名 | 描述 | 消息类型 |
|--------|------|----------|
| `/cmd_vel` | 速度指令 | `geometry_msgs::Twist` |

### 5.2 使用方法

#### 5.2.1 ROS1

1. 打开一个新的终端执行以下命令，以查看通信功能运行状态：
   ```bash
   sudo systemctl status transfer
   ```
   
   a. 若状态显示为active，则可直接使用通信功能；
   b. 若未运行，启动通信功能：
      ```bash
      sudo systemctl start transfer
      ```
   c. 如需关闭通信功能，则在终端中输入：
      ```bash
      sudo systemctl stop transfer
      ```
   d. 如需查看通信功能运行日志，则在终端中输入：
      ```bash
      journalctl -fu transfer
      ```

2. 查看话题信息：
   ```bash
   rostopic info xxxxxx
   rostopic echo xxxxxx     # xxxxxx指的是具体话题名称，可在自己的代码中订阅话题进行二次开发
   ```

3. 发布速度指令：
   
   **消息类型说明：**
   ```
   geometry_msgs/Vector3 linear     # 线速度(m/s)
   float64 z                        # 转向角速度，左转为正
   ```
   
   a. 基础命令：
      ```bash
      rostopic pub /cmd_vel geometry_msgs/Twist
      ```
   
   b. 输入完先不运行，在语句后面加一个空格，再按Tab键，就会自动补充当前消息类型的内容：
      ```bash
      rostopic pub /cmd_vel geometry_msgs/Twist "linear:
        x: 0.0
        y: 0.0
        z: 0.0
      angular:
        x: 0.0
        y: 0.0
        z: 0.0
      "
      ```
   
   c. 使用键盘上的向左/向右方向键移动光标修改速度值，然后在`geometry_msgs/Twist`之后加入 `-r 10` 规定发布频率(即每秒发布10次)：
      ```bash
      rostopic pub /cmd_vel geometry_msgs/Twist -r 10 "linear:
        x: 0.2
        y: 0.1
        z: 0.0
      angular:
        x: 0.0
        y: 0.0
        z: 0.3
      "
      ```
   
   d. 运行命令，即可发布话题。
   e. 传输程序会订阅该话题，并将其转为UDP指令消息发给运动主机。
   f. 传输程序正常开启后，使机器狗起立，并启动APP设置页中的自主模式，机器狗即可按照如上速度行动。

#### 5.2.2 ROS2

1. 从桌面打开一个新的终端执行以下命令，以查看通信功能运行状态：
   ```bash
   sudo systemctl status transfer_ros2
   ```
   
   a. 若状态显示为active，则可直接使用通信功能；
   b. 若未运行，启动通信功能：
      ```bash
      sudo systemctl start transfer_ros2
      ```
   c. 如需关闭通信功能，则在终端中输入：
      ```bash
      sudo systemctl stop transfer_ros2
      ```
   d. 如需查看通信功能运行日志，则在终端中输入：
      ```bash
      journalctl -fu transfer_ros2
      ```

2. 查看话题信息：
   ```bash
   ros2 topic info xxxxxx
   ros2 topic echo xxxxxx
   ```

3. 发布速度指令：
   ```bash
   ros2 topic pub -r 10 /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.2, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
   ```
   
   a. 用户可在基于ROS2编译的C++和Python程序中发布该话题(ROS基础的学习请参考[ROS2官方教程](https://docs.ros.org/en/foxy/Tutorials.html))，也可以打开一个终端，输入命令发布进行调试。
   b. 运行命令，即可发布话题。
   c. 传输程序会订阅该话题，并将其转为UDP指令消息发给运动主机。
   d. 传输程序正常开启后，使机器狗起立，并启动APP设置页中的自主模式，机器狗即可按照如上速度行动。

### 5.3 程序结构说明

#### 5.3.1 ROS1

```bash
/home/ysc/lite_cog/transfer/src
└── message_transformer
    ├── CMakeLists.txt
    ├── include
    │   ├── protocol.h
    │   └── sensor_logger.h
    ├── launch
    │   └── message_transformer.launch
    ├── msg
    │   ├── SimpleCMD.msg
    │   └── ComplexCMD.msg
    ├── package.xml
    └── src
```

主要代码结构：

1. 消息定义：
   ```cpp
   class SimpleCMD{
   };
   ```

2. 发布话题：
   ```cpp
   leg_odom_pub_ = nh.advertise<geometry_msgs::PoseWithCovarianceStamped>("leg_odom", 1);    
   ultrasound_pub_ = nh.advertise<std_msgs::Float64>("/us_publisher/ultrasound_distance", 1);
   ```

3. 订阅话题：
   ```cpp
   ros::Subscriber vel_sub = nh.subscribe("cmd_vel", 1, &ROS2QNX::CmdVelCallback, &ros2qnx);
   ros::Subscriber complexcmd_sub = nh.subscribe("complex_cmd", 1, &ROS2QNX::ComplexCMDCallback, &ros2qnx);
   ```

#### 5.3.2 ROS2

```bash
ysc@lite:~/lite_cog_ros2/transfer/src$ tree .
└── transfer_interfaces
    ├── CMakeLists.txt
    ├── msg
    │   ├── MotionComplexCMD.msg
    │   └── MotionSimpleCMD.msg
    └── package.xml
```

主要代码结构：

1. 消息定义：
   ```cpp
   class SimpleCMD{
   };
   ```

2. 发布话题：
   ```cpp
   leg_odom_pub_ = create_publisher<geometry_msgs::msg::PoseWithCovarianceStamped>("leg_odom", 10);    
   ultrasound_pub_ = create_publisher<std_msgs::msg::Float64>("/us_publisher/ultrasound_distance", 10);
   ```

3. 订阅话题：
   ```cpp
   cmd_vel_sub_ = create_subscription<geometry_msgs::msg::Twist>(
       "cmd_vel", 10, 
       std::bind(&MotionSender::CmdVelCallback, this, std::placeholders::_1));
   complexcmd_sub = create_subscription<transfer_interfaces::msg::MotionComplexCMD>(
       "complex_cmd", 10,
       std::bind(&MotionSender::ComplexCMDCallback, this, std::placeholders::_1));
   ```

---

## 6 识别跟随功能包

### 6.1 功能介绍

本案例基于DeepStream、Yolov8和TensorRT进行相机视频流获取、人体识别和目标追踪，计算目标位置并下发给控制模块，实现了机器狗的人体自动识别跟随功能。基于DeepStream硬解码获取h264编码的720p分辨率rtsp视频流，使用TensorRT加速Yolov8人体识别模型以识别开阔的场景中的人，最终识别帧率能达到20fps左右，追踪帧率能达到10fps左右。

- [Ultralytics YOLOv8](https://github.com/ultralytics/ultralytics)
- [Ultralytics Hub SDK](https://github.com/ultralytics/hub-sdk)

### 6.2 使用方法

1. 打开一个终端输入以下命令，以启动跟随程序：
   ```bash
   cd /home/ysc/lite_cog/track/src    #ROS1
   python3 run_tracker.py
   ```
2. 使用APP使机器狗起立并启动自主模式。
3. 当画面中出现人时，系统会为识别到的所有人物分配数字编号，并显示在画面上，用键盘输入需要跟随的人物的编号，按回车锁定对象。
4. 随后机器狗将对目标进行追踪、识别。
5. 跟随期间或目标丢失显示"Miss Object"时可以按回车进行重置。
6. 在终端中按下 `CTRL+C` 键结束程序。

### 6.3 二次开发说明

程序结构：
```
people_tracking
└── src
    ├── GStreamerWrapper
    │   └── GStreamerWrapper.py
    ├── hub_sdk
    ├── RobotController
    │   ├── FpsCounter
    │   │   └── FpsCounter.py
    │   ├── RobotController.py
    │   ├── ROSTransfer
    │   │   ├── ROS1Transfer.py
    │   │   ├── ROS2Transfer.py
    │   │   └── TransferConstants.py
    │   └── YoloWrapper
    │       ├── CocoTypeId.py
    │       └── YoloWrapper.py
    ├── run_tracker.py
    ├── test
    │   ├── pull.py
    │   ├── pull.sh
    │   └── yolov8.py
    └── ultralytics
```

主要说明：
1. `RobotController.py`的主要运行逻辑体现在其`Run()`函数中，用于对从视频流中获取的图像进行人体识别，而后将识别结果用于发送运动指令和图像画面交互控件绘制。

---

## 7 基于激光雷达的建图定位导航避障（ROS1）

### 7.1 建图（适用于 v3.1.04 及以后）

#### 7.1.1 功能介绍

[SLAM建图方案](https://github.com)

#### 7.1.2 程序结构

```
/home/ysc/lite_cog/system/scripts/slam
```

#### 7.1.3 使用方法

1. 打开终端输入以下启动对应的激光雷达驱动：
   ```bash
   cd /home/ysc/lite_cog/system/scripts/lidar
   ./start_rslidar.sh    #如果是Robosense雷达，请运行该脚本
   ```
   
   若雷达节点启动失败，可使用以下命令检查雷达与感知主机是否建立通信连接：
   ```bash
   ping 192.168.1.201
   ```
   
   > **【注意】** 建图过程中应保持该终端开启。

2. 启动点云建图程序：
   
   a. 创建启动脚本：
      ```bash
      #!/bin/sh
      gnome-terminal -x bash -c "bash /home/ysc/lite_cog/system/scripts/slam/save_map.sh; read -p 'Press any key to exit...'"
      ```
   
   b. 用户在使用NoMachine登录感知主机桌面，并遥控机器狗起立后，可在感知主机中打开一个终端输入以下命令，以使用该脚本启动建图程序：
      ```bash
      cd /home/ysc/lite_cog/system/scripts/slam
      ./start_slam.sh
      ```
   
   c. 执行完上述命令之后会启动可视化工具Rviz，并在运行建图启动脚本的终端中生成3个终端标签页，分别用来运行faster-lio，生成栅格地图和保存栅格地图。

3. 遥控机器狗环绕需要构建地图的区域行走，为了建图效果，在遥控转向时，请尽量保持转速缓慢。另外，由于激光雷达存在盲区，请让机器狗与墙面保持至少0.5米距离。

4. 在机器狗走完需要建图的区域后，在Rviz中查看建立的点云地图是否与真实环境符合。

5. 点云地图扫描完成后，找到faster-lio程序对应的标签页，按下ctrl+c以停止建图，程序将自动保存三维点云文件（.pcd）到`~/lite_cog/system/map`目录下，并显示统计得到的平均处理耗时（耗时仅供参考，具体数值可能有所浮动），再次按下回车键可关闭标签页。

6. 三维点云文件（.pcd）保存成功后，找到如下图所示的标签页，输入1并按回车键后稍等一会，此时会调用`pcd_2_gridmap`功能包将点云地图转化为栅格地图，当弹出RViz窗口显示栅格地图时可进行下一步。

7. 在点云地图转化为栅格地图之后，找到如下图所示的标签页，输入数字2并按回车键后稍等一会，此时会调用`map_server`功能包将栅格地图保存到`/home/ysc/lite_cog/system`目录下的map文件夹中（包括.yaml文件、.pgm文件）。

8. 若栅格地图不完全符合实际环境或需要人为划定可通行区域，可打开一个终端输入`gimp`以打开修图软件，并将`/home/ysc/lite_cog/system/map`目录下的.pgm文件拖入修图软件中进行修图。
   
   a. 若GIMP Image Editor中的工具栏没有自动打开，可在上方菜单栏中选择Windows - New Toolbox打开工具栏。
   b. 工具栏内含前景色与背景色工具，前景色设置铅笔工具的颜色，背景色设置橡皮擦工具的颜色。栅格地图中规定黑色为障碍物，即不可通行区域，白色为可通行区域，灰色为未知区域。您可以使用铅笔工具或橡皮擦工具添加或擦除黑色区域。
   c. 修改完成后在上方菜单栏选择File - Overwrite usr_map.pgm对原文件进行覆盖（请勿选择 File - Save保存），关闭该软件时会再次出现保存提示，此时无需再保存。

9. 完成所有操作后请用 Ctrl+C 关闭所有终端，以免影响后续进程。

10. 建好的地图文件将默认存在`/home/ysc/lite_cog/system`路径下的map文件中。如改变地图路径或名称，需要进行下述步骤，以便后续定位导航程序能够正确调用：
    
    a. 在对应的.yaml文件中更新地图路径（如与.pgm文件在同一路径下，则只需填写.pgm文件名称），并修改.yaml文件名使其与对应.pgm文件名字保持一致：
       ```yaml
       image:/home/ysc/lite_cog/system/map.pgm              // path of map file (.pgm)
       ...
       ```
    b. 在`~/lite_cog/nav/src/hdl_localization/launch`路径下的`local_rslidar_imu.launch`文件中配置所需地图名称和路径：
       ```xml
       <arg name="map_name" default="lite3" />       //Define Map File Name
       ...
       ```

### 7.2 建图（适用于 v3.1.04 以前）

#### 7.2.1 功能介绍

[六自由度三维激光SLAM算法的室内建图方案](https://github.com)

建图的主要操作流程如下所示：

相应的数据流图如下所示：

#### 7.2.2 程序结构

```
/home/ysc/lite_cog/slam
└── version
```

#### 7.2.3 使用方法

1. 打开终端输入以下启动对应的激光雷达驱动：
   ```bash
   cd /home/ysc/lite_cog/system/scripts/lidar
   ./start_rslidar.sh    #如果是Robosense雷达，请运行该脚本
   ```
   
   若雷达节点启动失败，可使用以下命令检查雷达与感知主机是否建立通信连接：
   ```bash
   ping 192.168.1.201
   ```

2. 启动点云建图程序：
   
   a. 创建启动脚本：
      ```bash
      #!/bin/sh
      gnome-terminal -x bash -c "bash /home/ysc/lite_cog/system/scripts/slam/save_map.sh"
      ```
   
   b. 用户在使用NoMachine登录感知主机桌面，并遥控机器狗起立后，可在感知主机中打开一个终端输入以下命令，以使用该脚本启动建图程序：
      ```bash
      cd /home/ysc/lite_cog/system/scripts/slam
      ./start_slam.sh
      ```
   
   c. 执行完上述命令之后会生成五个终端窗口，分别为运行建图脚本的终端，建图程序运行的终端，rviz开启终端，生成栅格地图的终端，保存地图的终端。

3. 若要在建图过程中实时看到建图效果，找到RViz可视化节点对应的终端（如下图所示），输入数字1并按回车键，此时会打开RViz可视化界面（打开此界面会降低建图性能，若后面对建图效果不满意，请尝试不打开可视化，并在建图时关闭NoMachine远程界面，减少算力资源占用）。

4. 遥控机器狗环绕需要构建地图的区域行走，为了建图效果，在遥控转向时，请尽量保持转速缓慢。另外，由于激光雷达存在盲区，请让机器狗与墙面保持至少0.5米距离。

5. 在机器狗走完需要建图的区域后，查看建立的点云地图是否与真实环境符合（若之前未打开RViz可视化界面，则在此时打开），若地图较大或存在回环（例如绕了房子一圈），请查看回环处的地图是否重合，若不重合，可以再绕一圈，使程序进一步完成回环检测。

6. 点云地图扫描完成后，找到grid map节点对应的终端（如下图所示）输入2并按回车键后稍等一会，此时会调用`octomap`功能包将点云地图转化为栅格地图。

7. 在点云地图转化为栅格地图之后，找到save map对应的节点终端（如下图所示）输入数字3并按回车键后稍等一会，此时会调用`map_server`功能包保存栅格地图，相关文件将保存到`/home/ysc/lite_cog/system`目录下的map文件夹中（包括.yaml文件、.pgm文件以及.pcd文件）。

8. 若栅格地图不完全符合实际环境或需要人为划定可通行区域，可打开一个终端输入`gimp`以打开修图软件，并将`/home/ysc/lite_cog/system/map`目录下的.pgm文件拖入修图软件中进行修图。
   
   a. 若GIMP Image Editor中的工具栏没有自动打开，可在上方菜单栏中选择Windows - New Toolbox打开工具栏。
   b. 工具栏内含前景色与背景色工具，前景色设置铅笔工具的颜色，背景色设置橡皮擦工具的颜色。栅格地图中规定黑色为障碍物，即不可通行区域，白色为可通行区域，灰色为未知区域。
   c. 修改完成后在上方菜单栏选择File - Overwrite usr_map.pgm对原文件进行覆盖（请勿选择 File - Save 保存）。

9. 完成所有操作后请用 `ctrl+c`关闭所有终端，以免影响后续进程。

10. 建好的地图文件将默认存在`/home/ysc/lite_cog/system`路径下的map文件中。如改变地图路径或名称，需要进行下述步骤：
    
    a. 在对应的.yaml文件中更新地图路径：
       ```yaml
       image:/home/ysc/lite_cog/system/map.pgm             // path of map file (.pgm)
       ...
       ```
    b. 在`~/lite_cog/nav/src/hdl_localization/launch`路径下的`local_rslidar_imu.launch`文件中配置所需地图名称和路径：
       ```xml
       <arg name="map_name" default="lite3" />      //Define Map File Name
       ...
       ```

### 7.3 定位导航

[hdl_localization定位算法](https://github.com)

#### 7.3.1 单目标点定位导航使用方法

1. 打开一个终端，输入以下指令以启动对应的雷达驱动（如果在建图中已经启动雷达驱动则无需再次启动）。
   ```bash
   cd /home/ysc/lite_cog/system/scripts/lidar
   ./start_rslidar.sh    #如果是Robosense雷达，请运行该脚本
   ```

2. 打开一个终端，输入以下指令以启动定位导航节点。
   ```bash
   cd /home/ysc/lite_cog/system/scripts/nav
   ./start_nav.sh
   ```

3. 开启导航后，需要对机器狗定位进行初始化：
   
   a. 点击顶部工具栏中的"2D Pose Estimate"按钮；
   b. 确定机器狗的实际位置和朝向，将鼠标移动到地图中的对应位置，按住左键并依照实际朝向拖拽出箭头，然后松开鼠标左键；
   c. 若定位初始化成功，激光点云与栅格地图会重合，且终端打印"initial pose received!!"；
   d. 若激光点云与栅格地图没有重合，则说明初始位置没给对，请重新操作；
   e. 若地图上没有出现激光点云，且终端打印的是"globalmap has not been received!!"，请用ctrl+c关闭程序，重新运行尝试。
   f. base_link坐标系为机器狗坐标系，x轴（红色）表示机器狗朝向。

4. 定位初始化成功后，可按照相似方法给定一个目标点：
   
   a. 点击顶部工具栏中的"2D Nav Goal"按钮；
   b. 将鼠标移动到地图中的目标位置，按住左键并依照目标朝向拖拽出箭头，然后松开鼠标；
   c. 若目标点指定成功，界面中会出现规划好的机器狗运动路径；若目标点指定失败，重新操作即可。

5. 在APP上打开机器狗的自主模式，并使机器狗起立，机器狗将按照全局规划得到的大致路径前进，并通过局部规划避开动态障碍物，最终到达目的地。

> **【注意】** 为防止机器狗身体被认定为障碍物，只有达到一定高度的物体才会被认定为障碍物。

> **【RViz使用技巧】** RViz中，用鼠标滚轮可放大缩小，用鼠标左键可旋转地图，按住shift并使用鼠标左键，可平移拖动地图。

#### 7.3.2 多目标点定位导航使用方法

本案例还提供了使机器狗按序到达一系列目标点进行循环导航的功能。

1. 首先参照7.3.1中的第1步至第3步启动导航程序并给定初始定位，然后再打开一个终端，运行以下指令开启目标点配置程序：
   ```bash
   cd /home/ysc/lite_cog/pipeline/src/pipeline_tracking/tools
   python3 location_record_en.py #英文版
   # 或
   python3 location_record.py #中文版
   ```

2. 在弹出的界面上添加目标点。

3. 打开一个终端，运行以下命令，并在APP上将机器狗的自主模式打开，机器狗将前往距离最近的记忆位点，并按照位点序号循环导航。
   ```bash
   cd /home/ysc/lite_cog/pipeline
   python3 Task.py
   ```

4. 此后，再次使用时，只需要先参照7.3.1启动导航程序并给定初始定位，然后执行第3步，即可使机器狗按照先前记录的目标点进行循环导航。

### 7.4 二次开发说明

#### 7.4.1 雷达驱动二次开发

雷达驱动位于`~/lite_cog/driver/leishen_ws/`和`~/lite_cog/driver/mid360_ws/`以及`~/lite_cog/driver/rslidar_sdk/`目录下。用户可自行阅读源码进行二次开发。

#### 7.4.2 SLAM建图二次开发

SLAM建图程序通过`/home/ysc/lite_cog/system/scripts/slam`文件夹下的`start_slam.sh`文件进行启动，`start_slam.sh`文件中第一行为启动建图程序，相应的launch文件中包含参数配置文件，其中主要包含点云预处理参数、点云配准算法参数、点云保存参数等，用户可自行调整。

#### 7.4.3 定位导航二次开发

1. 定位二次开发：定位导航程序通过`/home/ysc/lite_cog/system/scripts/nav`文件夹下的`start_nav.sh`文件进行启动，通过`start_nav.sh`文件中的内容可以找到定位启动的launch文件，其中主要包含点云预处理参数和点云配准参数，用户可自行调整。

相关文档：
- [global_planner文档](https://github.com)
- [teb_local_planner文档](https://github.com)
- [costmap_2d文档](https://github.com)
- [move_base文档](https://github.com)

---

## 8 基于激光雷达的建图定位导航避障（ROS2）

### 8.1 建图

#### 8.1.1 功能介绍

[SLAM建图方案](https://github.com)

建图的主要操作流程和数据流图如下所示：

#### 8.1.2 程序结构

```bash
ysc@lite:~/lite_cog_ros2/slam/src$ tree .
└── pcd2grid
```

#### 8.1.3 使用方法

1. 打开终端输入以下启动对应的激光雷达驱动：
   ```bash
   cd ~/lite_cog_ros2/system/scripts/lidar
   ./start_rslidar.sh    #如果是Robosense雷达，请运行该脚本
   ```
   
   若雷达节点启动失败，可使用以下命令检查雷达与感知主机是否建立通信连接：
   ```bash
   ping 192.168.1.201
   ```

2. 启动点云建图程序：
   
   a. 创建启动脚本：
      ```bash
      #!/bin/sh
      gnome-terminal -x bash -c "bash /home/ysc/lite_cog_ros2/system/scripts/slam/save_map.sh; read -p 'Press any key to exit...'; read -p 'Press Enter to exit...'"
      ```
   
   b. 用户在使用NoMachine登录感知主机桌面，并遥控机器狗起立后，可在感知主机中打开一个终端输入以下命令，以使用该脚本启动建图程序：
      ```bash
      cd ~/lite_cog_ros2/system/scripts/slam
      ./start_slam.sh
      ```
   
   c. 执行完上述命令之后会生成四个终端窗口，分别为运行建图脚本的终端，建图程序运行的终端，生成栅格地图的终端，保存地图的终端。

3. 遥控机器狗环绕需要构建地图的区域行走，为了建图效果，在遥控转向时，请尽量保持转速缓慢。另外，由于激光雷达存在盲区，请让机器狗与墙面保持至少0.5米距离。

4. 在机器狗走完需要建图的区域后，在Rviz中查看建立的点云地图是否与真实环境符合。

5. 点云地图扫描完成后，找到faster-lio程序对应的中断输入Ctrl+C停止建图，程序将自动保存pcd文件到`~/lite_cog_ros2/system/map`目录下，并显示统计得到的平均处理耗时（耗时仅供参考，具体数值可能有所浮动），再次按下回车键可退出终端。

6. pcd文件保存成功后，找到grid map节点对应的终端（如下图所示）输入1并按回车键后稍等一会，此时会调用`pcd_2_gridmap`功能包将点云地图转化为栅格地图，当弹出RViz窗口显示栅格地图时可进行下一步。

7. 在点云地图转化为栅格地图之后，找到save map对应的节点终端（如下图所示）输入数字2并按回车键后稍等一会，此时会调用`map_server`功能包保存栅格地图，文件将保存到`/home/ysc/lite_cog_ros2/system`目录下的map文件夹中（包括.yaml文件、.pgm文件）。

8. 若栅格地图不完全符合实际环境或需要人为划定可通行区域，可打开一个终端输入`gimp`以打开修图软件，并将`/home/ysc/lite_cog_ros2/system/map`目录下的.pgm文件拖入修图软件中进行修图。
   
   a. 若GIMP Image Editor中的工具栏没有自动打开，可在上方菜单栏中选择Windows - New Toolbox打开工具栏。
   b. 工具栏内含前景色与背景色工具，前景色设置铅笔工具的颜色，背景色设置橡皮擦工具的颜色。栅格地图中规定黑色为障碍物，即不可通行区域，白色为可通行区域，灰色为未知区域。
   c. 修改完成后在上方菜单栏选择File - Overwrite usr_map.pgm对原文件进行覆盖（请勿选择 File - Save保存）。

9. 完成所有操作后请用 Ctrl+C 关闭所有终端，以免影响后续进程。

10. 建好的地图文件将默认存在`/home/ysc/lite_cog_ros2/system`路径下的map文件中。如改变地图路径或名称，需要进行下述步骤：
    
    a. 在对应的.yaml文件中更新地图路径：
       ```yaml
       image:/home/ysc/lite_cog_ros2/system/map.pgm        // path of map file (.pgm)
       ...
       ```
    b. 在`~/lite_cog_ros2/nav/src/hdl_localization/launch`路径下的`lite_localization.launch.py`文件中配置所需地图名称和路径：
       ```python
       declare_map_server_config_file_cmd = DeclareLaunchArgument(
           {"globalmap_pcd": "/home/ysc/lite_cog_ros2/system/map/lite3.pcd"},
       ...
       ```

### 8.2 定位导航

[hdl_localization定位算法](https://github.com)

#### 8.2.1 单目标点定位导航使用方法

1. 打开一个终端，输入以下指令以启动对应的雷达驱动（如果在建图中已经启动雷达驱动则无需再次启动）。
   ```bash
   cd ~/lite_cog_ros2/system/scripts/lidar
   ./start_rslidar.sh    #如果是Robosense雷达，请运行该脚本
   ```

2. 打开一个终端，输入以下指令以启动定位导航节点。
   ```bash
   cd /home/ysc/lite_cog_ros2/system/scripts/nav
   ./start_nav.sh
   ```

3. 开启导航后，需要对机器狗定位进行初始化：
   
   a. 点击顶部工具栏中的"2D Pose Estimate"按钮；
   b. 确定机器狗的实际位置和朝向，将鼠标移动到地图中的对应位置，按住左键并依照实际朝向拖拽出箭头，然后松开鼠标左键；
   c. 若定位初始化成功，激光点云与栅格地图会重合，且终端打印"initial pose received!!"；
   d. 若激光点云与栅格地图没有重合，则说明初始位置没给对，请重新操作；
   e. 若地图上没有出现激光点云，且终端打印的是"globalmap has not been received!!"，请用ctrl+c关闭程序，重新运行尝试。
   f. base_link坐标系为机器狗坐标系，x轴（红色）表示机器狗朝向。

4. 定位初始化成功后，可按照相似方法给定一个目标点：
   
   a. 点击顶部工具栏中的"2D Nav Goal"按钮；
   b. 将鼠标移动到地图中的目标位置，按住左键并依照目标朝向拖拽出箭头，然后松开鼠标；
   c. 若目标点指定成功，界面中会出现规划好的机器狗运动路径；若目标点指定失败，重新操作即可。

5. 在APP上打开机器狗的自主模式，并使机器狗起立，机器狗将按照全局规划得到的大致路径前进，并通过局部规划避开动态障碍物，最终到达目的地。

> **【注意】** 为防止机器狗身体被认定为障碍物，只有达到一定高度的物体才会被认定为障碍物。

#### 8.2.2 多目标点定位导航使用方法

本案例还提供了使机器狗按序到达一系列目标点进行循环导航的功能。

> **【注意】** 开始标点前，请检查`/home/ysc/lite_cog_ros2/pipeline/src/pipeline/data`文件夹中是否存在以前记录的目标点文件，如有，可移动到其他文件夹，以避免覆盖。

1. 首先参照8.2.1中的第1步至第3步启动导航程序并给定初始定位，然后再打开一个终端，运行以下指令开启目标点配置程序：
   
   a. 如需打开中文版界面，请运行：
      ```bash
      cd ~/lite_cog_ros2/pipeline/src/pipeline
      python3 LocationRecorder.py 
      ```
   
   b. 如需打开英文版界面，请运行：
      ```bash
      cd ~/lite_cog_ros2/pipeline/src/pipeline
      python3 LocationRecorder_EN.py
      ```

2. 在弹出的界面上添加目标点。

3. 打开一个终端，运行以下命令，并在APP上将机器狗的自主模式打开，机器狗将前往距离最近的记忆位点，并按照位点序号循环导航。
   ```bash
   cd /home/ysc/lite_cog_ros2/pipeline/src/pipeline
   python3 Task.py
   ```

4. 此后，再次使用时，只需要执行第1步和第3步，即可使机器狗按照先前记录的目标点进行循环导航。

### 8.3 二次开发说明

#### 8.3.1 雷达驱动二次开发

雷达驱动位于`~/lite_cog_ros2/driver/leishen_ws/`和`~/lite_cog_ros2/driver/mid360_ws/`以及`~/lite_cog_ros2/driver/rslidar_sdk/`目录下。用户可自行阅读源码进行二次开发。

#### 8.3.2 SLAM建图二次开发

SLAM建图程序通过`/home/ysc/lite_cog_ros2/system/scripts/slam`文件夹下的`start_slam.sh`文件进行启动，`start_slam.sh`文件中第一行为启动建图程序，相应的launch文件中包含参数配置文件，其中主要包含点云预处理参数、点云配准算法参数、点云保存参数等，用户可自行调整。

#### 8.3.3 定位导航二次开发

1. 定位二次开发：定位导航程序通过`/home/ysc/lite_cog_ros2/system/scripts/nav`文件夹下的`start_nav.sh`文件进行启动，通过`start_nav.sh`文件中的内容可以找到定位启动的launch文件，其中主要包含点云预处理参数和点云配准参数，用户可自行调整。

2. 导航二次开发：导航相关文档请参考[navigation2官方文档](https://github.com)。

---

*文档编制: 陈伟 | 版本: V2.2.3 | 编制日期: 2026-08-20*
