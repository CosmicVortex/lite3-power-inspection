# 远程部署与监测平台指南

> **文档编号**: DEPLOY-001  
> **版本**: V1.0  
> **编制日期**: 2026-08-19  
> **编制人**: 陈伟

---

## 一、网络拓扑设计

### 1.1 离线网络架构

由于绝影Lite3机器狗主机未接入互联网，采用**本地WiFi热点模式**进行远程访问：

```
┌─────────────────────────────────────────────────────────────┐
│                      现场演示环境                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌──────────────┐    WiFi    ┌──────────────┐             │
│   │  笔记本电脑   │ ◄────────► │  感知主机     │             │
│   │  (192.168.1.1)│            │  (Jetson NX) │             │
│   └──────┬───────┘            │  (192.168.1.120)│            │
│          │                     └──────┬───────┘             │
│          │ WiFi热点                   │ UDP/WebSocket       │
│          │                            │                    │
│          ▼                     ┌──────▼───────┐            │
│   ┌──────────────┐            │  运动主机     │            │
│   │  监测平台     │◄──────────►│  (RK3588)     │            │
│   │ (Web UI)     │  HTTP      │  (192.168.1.103)│           │
│   └──────────────┘            └──────┬───────┘            │
│                                     │                     │
│                              ┌──────▼───────┐             │
│                              │  数尔云台     │             │
│                              │  (192.168.1.108)│           │
│                              └──────────────┘             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 IP地址规划

| 设备 | IP地址 | 用途 |
|------|--------|------|
| 笔记本电脑 | 192.168.1.1 | 监测平台、SSH客户端 |
| 感知主机 (Jetson NX) | 192.168.1.120 | 视觉算法、数据上报 |
| 运动主机 (RK3588) | 192.168.1.103 | 运动控制 |
| 数尔云台 | 192.168.1.108 | 视频采集、云台控制 |
| 监测平台服务器 | 192.168.1.200 | 数据接收（可选） |

---

## 二、远程访问方案

### 2.1 方案A：SSH远程登录（推荐）

#### 步骤1：笔记本电脑配置WiFi热点

```bash
# Ubuntu系统创建WiFi热点
sudo nmcli device wifi hotspot ifname wlan0 ssid "Lite3-Deploy" password "deploy123"

# 查看本机IP地址
ip addr show
# 记录 wlan0 的IP地址，例如: 192.168.1.1
```

#### 步骤2：配置感知主机网络

```bash
# 在Jetson NX上配置静态IP
sudo nano /etc/netplan/01-netcfg.yaml

# 添加以下内容
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: no
      addresses:
        - 192.168.1.120/24
      nameservers:
        addresses:
          - 8.8.8.8
      routes:
        - to: default
          via: 192.168.1.1

# 应用配置
sudo netplan apply
```

#### 步骤3：SSH登录

```bash
# 从笔记本电脑SSH登录到感知主机
ssh admin@192.168.1.120

# 首次登录需修改密码
# 后续使用密钥认证（推荐）
ssh-copy-id admin@192.168.1.120
```

### 2.2 方案B：USB网络共享

如果WiFi热点不稳定，可使用USB以太网共享：

```bash
# Jetson NX端启用USB网络共享
sudo modprobe g_ether
sudo ifconfig usb0 192.168.1.120 netmask 255.255.255.0

# 笔记本电脑端配置
sudo ifconfig enp0s20 192.168.1.1 netmask 255.255.255.0
```

### 2.3 方案C：VNC远程桌面

如需图形界面操作：

```bash
# 在Jetson NX上安装VNC服务器
sudo apt-get install -y tigervnc-standalone-server tigervnc-common
vncserver :1 -geometry 1920x1080 -depth 24

# 从笔记本电脑连接
# 使用VNC客户端（如RealVNC、TightVNC）
# 连接地址: 192.168.1.120:5901
```

---

## 三、应用程序部署流程

### 3.1 部署脚本

创建自动化部署脚本 `scripts/deploy.sh`：

```bash
#!/bin/bash
# 绝影Lite3电力巡检系统部署脚本

set -e

REMOTE_HOST="admin@192.168.1.120"
PROJECT_DIR="/home/admin/lite3-power-inspection"
PYTHON_VENV="$PROJECT_DIR/venv"

echo "=========================================="
echo "绝影Lite3电力巡检系统部署"
echo "=========================================="

# 1. 检查网络连接
echo "[1/6] 检查网络连接..."
ping -c 2 $REMOTE_HOST > /dev/null 2>&1 || {
    echo "❌ 无法连接到 $REMOTE_HOST"
    exit 1
}
echo "✅ 网络连接正常"

# 2. 创建项目目录
echo "[2/6] 创建项目目录..."
ssh $REMOTE_HOST "mkdir -p $PROJECT_DIR"

# 3. 同步代码文件
echo "[3/6] 同步代码文件..."
rsync -avz --progress \
    --exclude='__pycache__' \
    --exclude='*.pyc' \
    --exclude='.git' \
    --exclude='venv' \
    --exclude='data/' \
    ./ $REMOTE_HOST:$PROJECT_DIR/
echo "✅ 代码同步完成"

# 4. 安装Python依赖
echo "[4/6] 安装Python依赖..."
ssh $REMOTE_HOST "cd $PROJECT_DIR && python3 -m venv $PYTHON_VENV && \
    source $PYTHON_VENV/bin/activate && \
    pip install -r requirements.txt --quiet"
echo "✅ 依赖安装完成"

# 5. 创建数据目录
echo "[5/6] 创建数据目录..."
ssh $REMOTE_HOST "mkdir -p $PROJECT_DIR/{data,logs,models}"
echo "✅ 数据目录创建完成"

# 6. 验证部署
echo "[6/6] 验证部署..."
ssh $REMOTE_HOST "cd $PROJECT_DIR && source $PYTHON_VENV/bin/activate && \
    python3 -c 'from src.perception.temperature_monitor import TemperatureMonitor; m = TemperatureMonitor(); print(\"✅ 温度监测模块加载成功\")'"
ssh $REMOTE_HOST "cd $PROJECT_DIR && source $PYTHON_VENV/bin/activate && \
    python3 -c 'from src.gateway.udp_controller import UDPMotionController; print(\"✅ UDP控制器加载成功\")'"
ssh $REMOTE_HOST "cd $PROJECT_DIR && source $PYTHON_VENV/bin/activate && \
    python3 -c 'from src.gateway.ptz_controller import PtzController; print(\"✅ 云台控制器加载成功\")'"
ssh $REMOTE_HOST "cd $PROJECT_DIR && source $PYTHON_VENV/bin/activate && \
    python3 -c 'from src.storage.sqlite_cache import SQLiteCache; c = SQLiteCache(); print(\"✅ SQLite缓存加载成功\")'"

echo ""
echo "=========================================="
echo "✅ 部署完成！"
echo "=========================================="
echo ""
echo "SSH登录: ssh $REMOTE_HOST"
echo "项目目录: $PROJECT_DIR"
echo "Python环境: $PYTHON_VENV/bin/python3"
echo ""
echo "启动监测平台:"
echo "  cd $PROJECT_DIR && source $PYTHON_VENV/bin/activate && python3 src/app/monitor_platform.py"
echo ""
echo "访问地址: http://192.168.1.1:5000"
```

### 3.2 执行部署

```bash
# 赋予执行权限
chmod +x scripts/deploy.sh

# 执行部署
./scripts/deploy.sh
```

---

## 四、基础功能独立测试

### 4.1 测试环境准备

在未加载视觉模型的情况下，可对以下基础功能进行独立测试：

| 功能模块 | 测试内容 | 测试方法 |
|----------|----------|----------|
| 温度监测 | 双级告警逻辑 | 模拟热成像数据 |
| 云台控制 | 角度/变焦控制 | 连接真实云台 |
| UDP通信 | 心跳/指令发送 | 连接运动主机 |
| WebSocket | 数据上报 | 连接监测平台 |
| RTSP流 | 视频拉取 | 连接云台相机 |
| SQLite存储 | 数据持久化 | 本地文件测试 |

### 4.2 温度监测测试

```python
# 运行测试
cd /home/admin/lite3-power-inspection
source venv/bin/activate

python3 -m src.perception.temperature_monitor
```

**预期输出：**
```
2026-08-19 10:00:00 | INFO | 初始化温度监测器: WARN>45.0℃, CRITICAL>50.0℃
2026-08-19 10:00:00 | DEBUG | 温度监测: max=48.0℃, mean=40.0℃, status=WARN
温度监测结果: {'status': 'WARN', 'temperature': 40.0, 'max_temperature': 48.0, 'alert_id': 'ALT-20260819-001', 'confirm_count': 1}
```

### 4.3 UDP通信测试

```python
# 运行测试
python3 -m src.gateway.udp_controller
```

**预期输出：**
```
2026-08-19 10:00:00 | INFO | 初始化UDP控制器: 192.168.1.103:43893
2026-08-19 10:00:00 | INFO | UDP连接测试成功
发送指令: 0x21040001, value=0
发送指令: 0x21010528, value=0
```

### 4.4 云台控制测试

```python
# 运行测试
python3 -m src.gateway.ptz_controller
```

**预期输出：**
```
2026-08-19 10:00:00 | INFO | 初始化云台控制器: http://192.168.1.108
2026-08-19 10:00:00 | INFO | 云台登录成功
云台状态: PtzState(yaw=45.0, pitch=-30.0, roll=0.0, zoom=10, is_enabled=True)
```

### 4.5 数据上报测试

```python
# 运行测试
python3 -m src.gateway.websocket_client
```

**预期输出：**
```
2026-08-19 10:00:00 | INFO | 初始化WebSocket网关: ws://192.168.1.200:8765/ws
2026-08-19 10:00:00 | INFO | WebSocket连接成功
发送消息: system_status
```

### 4.6 SQLite存储测试

```python
# 运行测试
python3 -m src.storage.sqlite_cache
```

**预期输出：**
```
2026-08-19 10:00:00 | INFO | SQLite缓存初始化: data/test.db
2026-08-19 10:00:00 | INFO | 数据库表初始化完成
保存结果ID: 1
保存告警ID: 1
未上传结果数: 1
告警历史数: 1
测试完成
```

---

## 五、监测平台部署

### 5.1 平台功能

监测平台提供以下核心功能：

| 功能模块 | 说明 |
|----------|------|
| 数据概览 | 实时显示检测统计、系统状态 |
| 趋势图表 | 温度变化趋势、裂缝尺寸分布 |
| 告警管理 | 实时告警列表、历史记录查询 |
| 数据可视化 | 检测结果表格、告警分布饼图 |
| 模拟测试 | 生成测试数据验证系统 |
| 日志监控 | 实时系统日志输出 |

### 5.2 启动监测平台

```bash
# 方法1：在感知主机上运行
cd /home/admin/lite3-power-inspection
source venv/bin/activate
python3 src/app/monitor_platform.py

# 访问地址: http://192.168.1.120:5000
```

```bash
# 方法2：在笔记本电脑上运行（推荐）
# 首先确保代码已同步
cd ~/lite3-power-inspection
source venv/bin/activate

# 启动监测平台
python3 src/app/monitor_platform.py

# 访问地址: http://192.168.1.1:5000
```

### 5.3 API接口说明

| 接口 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 监测平台主页 |
| `/api/status` | GET | 获取系统状态 |
| `/api/inspection_results` | GET | 获取检测结果列表 |
| `/api/alerts` | GET | 获取告警历史 |
| `/api/statistics` | GET | 获取统计数据 |
| `/api/telemetry` | POST | 接收遥测数据 |
| `/api/simulation` | POST | 模拟测试数据 |
| `/api/reset` | POST | 重置所有数据 |

---

## 六、完整测试流程

### 6.1 一键测试脚本

创建 `scripts/test_all.sh`：

```bash
#!/bin/bash
# 完整测试流程

set -e

PROJECT_DIR="/home/admin/lite3-power-inspection"
VENV="$PROJECT_DIR/venv/bin/activate"

echo "=========================================="
echo "绝影Lite3电力巡检系统 - 完整测试"
echo "=========================================="

# 1. 温度监测测试
echo ""
echo "[测试1/6] 温度监测模块..."
cd $PROJECT_DIR && source $VENV && python3 -m src.perception.temperature_monitor

# 2. UDP通信测试
echo ""
echo "[测试2/6] UDP运动控制..."
cd $PROJECT_DIR && source $VENV && python3 -m src.gateway.udp_controller

# 3. 云台控制测试
echo ""
echo "[测试3/6] 云台控制..."
cd $PROJECT_DIR && source $VENV && python3 -m src.gateway.ptz_controller

# 4. WebSocket测试
echo ""
echo "[测试4/6] WebSocket数据上报..."
cd $PROJECT_DIR && source $VENV && python3 -m src.gateway.websocket_client

# 5. SQLite存储测试
echo ""
echo "[测试5/6] SQLite本地缓存..."
cd $PROJECT_DIR && source $VENV && python3 -m src.storage.sqlite_cache

# 6. 监测平台测试
echo ""
echo "[测试6/6] 监测平台启动..."
echo "启动监测平台: http://192.168.1.1:5000"
echo "按 Ctrl+C 停止"
cd $PROJECT_DIR && source $VENV && python3 src/app/monitor_platform.py
```

### 6.2 执行测试

```bash
# 赋予执行权限
chmod +x scripts/test_all.sh

# 执行完整测试
./scripts/test_all.sh
```

---

## 七、故障排查

### 7.1 网络连接问题

| 现象 | 可能原因 | 解决方案 |
|------|----------|----------|
| SSH连接超时 | 防火墙拦截 | `sudo ufw allow 22/tcp` |
| IP地址冲突 | DHCP分配重复 | 使用静态IP配置 |
| DNS解析失败 | 无外网DNS | 配置本地hosts或使用IP地址 |

### 7.2 服务启动失败

| 现象 | 可能原因 | 解决方案 |
|------|----------|----------|
| Flask端口被占用 | 5000端口被占用 | 修改端口或杀死占用进程 |
| SQLite权限不足 | 数据目录权限问题 | `chmod 755 data/` |
| Python模块导入失败 | 依赖未安装 | 重新执行 `pip install -r requirements.txt` |

### 7.3 硬件连接问题

| 现象 | 可能原因 | 解决方案 |
|------|----------|----------|
| UDP心跳无响应 | 运动主机未启动 | 检查运动主机电源和网络 |
| 云台登录失败 | 密码错误 | 确认默认账号admin/123456 |
| RTSP流无法拉取 | 端口被占用 | 检查554端口和相机配置 |

---

## 八、总结

### 8.1 部署流程图

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ 笔记本电脑   │────►│  WiFi热点   │────►│  感知主机    │
│ (监测平台)   │     │ (192.168.1) │     │ (Jetson NX) │
└─────────────┘     └─────────────┘     └──────┬──────┘
                                               │
                    ┌──────────────────────────┼──────────────────────────┐
                    │                          │                          │
                    ▼                          ▼                          ▼
            ┌─────────────┐          ┌─────────────┐          ┌─────────────┐
            │  UDP通信     │          │  云台控制    │          │  WebSocket  │
            │ (运动主机)   │          │ (数尔云台)   │          │ (数据上报)   │
            └─────────────┘          └─────────────┘          └─────────────┘
                    │                          │                          │
                    └──────────────────────────┴──────────────────────────┘
                                               │
                                               ▼
                                      ┌─────────────┐
                                      │  SQLite缓存  │
                                      │ (本地存储)   │
                                      └─────────────┘
```

### 8.2 测试顺序建议

1. **第一阶段：基础功能测试**
   - 温度监测算法（可离线测试）
   - SQLite数据缓存（可离线测试）
   - UDP通信协议（需运动主机）

2. **第二阶段：硬件联调**
   - 云台控制（需连接云台）
   - RTSP视频流（需连接相机）
   - WebSocket数据上报（需监测平台）

3. **第三阶段：完整流程测试**
   - 监测平台可视化
   - 模拟数据测试
   - 端到端集成测试

---

*文档版本: V1.0 | 编制日期: 2026-08-19 | 编制人: 陈伟*
