# GPU加速依赖（可选，用于真实模型推理）
# 使用命令：pip install -r requirements-gpu.txt
# 
# 注意：
# - 需要NVIDIA GPU和CUDA环境
# - 仅在进行真实模型推理时需要
# - 模拟模式不需要此文件

# 深度学习框架
torch>=2.0.0
torchvision>=0.15.0

# TensorRT推理加速
tensorrt>=8.0.0

# ONNX模型支持
onnx>=1.12.0
onnxruntime-gpu>=1.14.0

# 视频处理
av>=10.0
ffmpeg-python>=0.2.0

# 性能监控
tqdm>=4.64.0
